# TaxEaseBD backend dependencies.
#
# Kept deliberately small: no torch/transformers/langchain/faiss/groq.
# Those belonged to an abandoned RAG experiment (backend/main.py used to be
# that script) that was never wired into the actual API and pulled in
# ~1.5GB of ML dependencies that frequently fail to install on Windows
# without build tools. The live /api/chat endpoint is a small curated
# knowledge base, not an LLM call, so none of that is needed to run the app.
#
# If you want to add real RAG (FAISS + embeddings + an LLM) back later,
# reintroduce those packages then - don't install them speculatively.

fastapi==0.128.8
uvicorn==0.39.0
sqlalchemy==2.0.51
pydantic==2.13.4
python-dotenv==1.2.1
PyJWT==2.9.0
pymysql==1.1.1
cryptography==44.0.1
